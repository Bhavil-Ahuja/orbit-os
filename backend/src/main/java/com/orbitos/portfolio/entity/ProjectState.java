package com.orbitos.portfolio.entity;

/**
 * Project publication state. Persisted as lowercase string (published, draft, archived).
 */
public enum ProjectState {
    PUBLISHED,
    DRAFT,
    ARCHIVED
}
